{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "bolivibes-web",
  "routes": [
    { "pattern": "bolivibes.clubemkt.digital", "custom_domain": true }
  ],
  "account_id": "cb27e1a67198789eb42d11ab90737652",
  "main": ".open-next/worker.js",
  // Update to the actual date you first deploy — must be >= 2024-09-23 for @opennextjs/cloudflare.
  "compatibility_date": "2026-08-21",
  "compatibility_flags": ["nodejs_compat"],
  "assets": {
    "directory": ".open-next/assets",
    "binding": "ASSETS"
  },
  "observability": {
    "enabled": true
  },
  // Created in the ClubeMKT Cloudflare account (cb27e1a67198789eb42d11ab90737652),
  // so the Worker and the clubemkt.digital DNS zone share one account.
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "bolivamos-db",
      "database_id": "d34573dd-84d2-4a31-be63-b011fbe3bc29",
      "migrations_dir": "../../packages/db/migrations"
    }
  ],
  // Created in the ClubeMKT Cloudflare account (cb27e1a67198789eb42d11ab90737652).
  "kv_namespaces": [
    {
      "binding": "BOLIVIBES_KV",
      "id": "0c314347008243f8a51afffc3aa3e404"
    }
  ],
  // Holds normalized/processed event banner images — see lib/event-image.ts.
  "r2_buckets": [
    {
      "binding": "EVENT_ASSETS",
      "bucket_name": "bolivamos-event-assets"
    }
  ],
  // Used to crop/resize/re-encode admin-pasted event banner images before
  // they're stored in R2 above — see lib/event-image.ts. Free up to 5,000
  // unique transformations/month, far more than admin event-editing volume.
  "images": {
    "binding": "IMAGES"
  },
  // Not secret — safe to keep in source. Secrets (JWT_SECRET, GOOGLE_CLIENT_SECRET,
  // GEMINI_API_KEY, BOLIVIA_AI_API_KEY, RESEND_API_KEY, ZERNIO_API_KEY) are set via `wrangler secret put`
  // and never appear here.
  "vars": {
    "GOOGLE_WEB_CLIENT_ID": "720746121278-lg8alamjpbgbhem2vcps1bulh7ng4e6j.apps.googleusercontent.com",
    "GOOGLE_IOS_CLIENT_ID": "",
    "GOOGLE_ANDROID_CLIENT_ID": "",
    "BOLIVIA_AI_ENDPOINT": "http://62.171.137.90",
    "NEXT_PUBLIC_GOOGLE_MAPS_API_KEY": "AIzaSyDOYaIIym1YjivwlEBI00TJAtKUHEjHMiY",
    "NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID": "7871140d830664e03d4d6561"
  }
}
